import { useState } from "react"

export default function Image (path) {
    const [img, setImg] = useState({})


    return <video> 
       <source src={`D:\\React Projects\\barad-dur\\src\\Warhammer.mkv`} />
    </video>;
}